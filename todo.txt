use UITableViewIndexSearch in the table manager to get the search icon
figure out scrolling behavior when switching tables in recipe view
add search to ingredients view
should not hide the hairline shadow when navigating deeper into the recipe view
why is the entire app's background black and can't I change it?
how to get the hairline shadow back when drilling farther down in the iew hierarchy?

design todos:
3 pages per recipe:
    1. full-size picture, notes (?), link (out to safari!)
    2. ingredients, instructions (ordered?), notes (?)
    3. similar drinks (automatically determined?)
question: page control dots, or segmented control with secondary swiping support?
